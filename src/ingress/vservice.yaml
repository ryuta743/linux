apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: web
spec:
  hosts:
    - "*"
  gateways:
    - web-gateway
  http:
    route:
      - destination:
          host: web
          port:
            number: 9080
