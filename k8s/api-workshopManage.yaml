apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-workshopManage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express-workshopManage
      version: v1
  template:
    metadata:
      labels:
        app: express-workshopManage
        version: v1
    spec:
      containers:
        - name: express-workshopManage-container
          image: ryutaterada/k8s-express:1.1.9
          resources:
            limits:
              cpu: "1"
              memory: 1000Mi
          ports:
            - containerPort: 5000
              name: express-workshopManage
---
apiVersion: v1
kind: Service
metadata:
  name: express-workshopManage-service
spec:
  type: ClusterIP
  selector:
    app: express-workshopManage
  ports:
    - port: 80
      targetPort: 5000
      name: http
